import { FormGroupControlPipe } from './form-group-control.pipe';

describe('FormGroupControlPipe', () => {
  it('create an instance', () => {
    const pipe = new FormGroupControlPipe();
    expect(pipe).toBeTruthy();
  });
});
